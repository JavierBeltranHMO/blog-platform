<%= form_with(model: blog) do |form| %>
  <% if blog.errors.any? %>
    <div class="alert alert-danger">
      <h4><%= pluralize(blog.errors.count, "error") %> prohibited this blog from being saved:</h4>
      <ul class="mb-0">
        <% blog.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<%# blog title %>
  <div class="mb-3">
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title, class:"form-control", required: true %>
  </div>
  <%# blog bosy %>
  <div class="mb-3">
    <%= form.label :body, style: "display: block" %>
    <%= form.rich_text_area :body, class:"form-control", rows:5, required: true %>
  </div>
      <%# Choose featured blogs %>
  <div class="form-check">
    <%= form.check_box :featured, class:"form-check-input" %>
    <%= form.label :featured, "Mark as featured", class:"form-check-label" %>
  </div>
  <div class="d-grid">
    <%= form.submit class:"btn btn-primary" %>
  </div>
<% end %>
